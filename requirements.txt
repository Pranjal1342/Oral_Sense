# ─────────────────────────────────────────────────────────────────────────────
# AFI Oral Cancer Screening — Requirements
# Hardware target: ESP32-S3 (training on host GPU / CPU only)
# Python: 3.9 – 3.11  (TF 2.13 not yet stable on 3.12)
# ─────────────────────────────────────────────────────────────────────────────

# Core deep-learning framework
tensorflow==2.13.1          # last stable release with Keras 2 API used by vit-keras

# Vision Transformer support (required for ViT-B/16 reference model)
vit-keras==0.1.2
tensorflow-addons>=0.21.0
setuptools<70.0.0
opencv-python==4.8.1.78

# Data / ML utilities
numpy>=1.23,<=1.24.3        # TF 2.13 is incompatible with NumPy > 1.24.3
pandas>=1.5,<3.0
scikit-learn>=1.2

# Statistical tests (paired t-test)
scipy>=1.10

# Plotting
matplotlib>=3.6

# Image augmentation (optional — used if you switch to Albumentations pipeline)
# albumentations>=1.3
